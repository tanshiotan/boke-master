# プロキシの中継先となるFastAPIのURL 未設定時は http://localhost:8000
# ブラウザはNext.jsへ同一オリジンで送り Next.jsがこのURLへ中継する
BACKEND_API_URL=http://localhost:8000

# プロキシを介さずブラウザから直接叩きたい場合のみ設定する
# 設定するとBACKEND_API_URLの中継より優先されCORS設定が必要になる
# NEXT_PUBLIC_API_URL=http://localhost:8000
